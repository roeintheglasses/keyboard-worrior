const express = require('express');


// Create a router for handling user requests
const apiRouter = express.Router();

// Define routes for the router
userRouter.get('/crawl', apiController.crawlStacksKb);
